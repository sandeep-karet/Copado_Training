<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Contact_Email_Parse__c</fullName>
    <description>Looks to email field and parses email domain information from the email address. This is utilized in automation surrounding new leads with no accounts.</description>
    <externalId>false</externalId>
    <formula>IF(Email__c&lt;&gt;NULL,SUBSTITUTE(Email__c, LEFT(Email__c, FIND(&quot;@&quot;, Email__c)), NULL),NULL)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Looks to email field and parses email domain information from the email address. This is utilized in automation surrounding new leads with no accounts.</inlineHelpText>
    <label>Contact Email Parse</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
