<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Active__c</fullName>
    <description>Checked if today’s date is &gt;= Contract Effective Date AND &lt;= Contract End Date</description>
    <externalId>false</externalId>
    <formula>AND( TODAY() &gt;= StartDate__c, TODAY() &lt;=  ContractRenewalDate__c )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Checked if today’s date is &gt;= Contract Effective Date AND &lt;= Contract End Date</inlineHelpText>
    <label>Active</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
